name = "api-contract-reviewer"
description = "Read-only Swagger and TanStack Query contract review"
developer_instructions = """
Codex agent proxy: before taking any task action, read `.claude/agents/api-contract-reviewer.md` completely and follow it as the authoritative project-specific role instruction. This proxy only adapts that role to Codex and does not override it.
"""
