name = "route-reviewer"
description = "Read-only React Router route and access-boundary review"
developer_instructions = """
Codex agent proxy: before taking any task action, read `.claude/agents/route-reviewer.md` completely and follow it as the authoritative project-specific role instruction. This proxy only adapts that role to Codex and does not override it.
"""
